steps:
  - label: "🔐 Load secret & Bootstrap service account"
    key: "load-and-bootstrap"
    agents:
      queue: "kubernetes"
    command: |
      echo "Loading kubeconfig secret..."
      export BUILDKITE_PLUGIN_K8S_BOOTSTRAP_CLUSTER_ADMIN_KUBECONFIG="$(buildkite-agent secret get k8s_admin_kubeconfig)"
      echo "Secret loaded, length: $(echo "$BUILDKITE_PLUGIN_K8S_BOOTSTRAP_CLUSTER_ADMIN_KUBECONFIG" | wc -c) chars"
    plugins:
      - Mykematt/k8s-bootstrap#v1.0.0:
          namespace: "default"
          service-account-name: "deployer-service-account"
          permissions:
            - deployments
            - pods
            - secrets
            - configmaps
            - services

  - label: "🚀 Deploy nginx application"
    key: "deploy-nginx"
    depends_on: "load-and-bootstrap"
    agents:
      queue: "kubernetes"
    command: |
      echo "==== STEP 3: Deploying Application ===="
      echo "Downloading scoped kubeconfig artifact from Step 1..."
      buildkite-agent artifact download "kubeconfig-*" .
      export KUBECONFIG="$(find . -name 'kubeconfig-*')"
      echo "Using scoped kubeconfig: $KUBECONFIG"
      echo "Applying Kubernetes manifests with limited permissions..."
      kubectl apply -f k8s/configmap.yaml
      kubectl apply -f k8s/deployment.yaml
      kubectl apply -f k8s/service.yaml
      echo "✅ Deployment completed successfully!"